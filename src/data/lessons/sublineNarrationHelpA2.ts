import type { SublineNarration } from '../../services/sublineLesson';

// HELPER A2 — overflow lane for the Indian-bucket DEEP sub-tries (David
// 2026-06-18). OVERRIDE LAYER (spread last → wins over a group's intro-only
// entry). Owned by the incoming KID/Sämisch session. Author the REMAINING
// sub-tries here (NOT the main system deviations already in HelpA.ts — grep
// HelpA first). Key: `${openingId}::${variationIndex}::${triggerMove}@${atPly}`.
export const SUBLINE_NARRATION_HELP_A2: Record<string, SublineNarration> = {};
