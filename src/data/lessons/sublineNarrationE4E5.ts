import type { SublineNarration } from '../../services/sublineLesson';

// GROUP A — 1.e4 e5 complex + e4 gambits. Owned by ONE parallel session.
// Author hand-written, board-verified subline narration here (see the WO).
// Key format: `${openingId}::${variationIndex}::${triggerMove}@${atPly}`.
// Add your own local helpers (KEY/ATK/SOFT/H/A) when you start authoring.
export const SUBLINE_NARRATION_E4E5: Record<string, SublineNarration> = {};
