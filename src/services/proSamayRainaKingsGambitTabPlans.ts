// Tab-plan resolver for the Samay Raina King's Gambit pro-rep (STEP 12.5).
// Variation tabs resolve to [] until plans are authored.
export function getProSamayRainaKingsGambitTabPlanIds(openingId: string, _tabKey: string): string[] | null {
  if (openingId !== 'pro-samayraina-kings-gambit') return null;
  return [];
}
