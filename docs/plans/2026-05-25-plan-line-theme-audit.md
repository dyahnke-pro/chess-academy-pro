# PLAN — Middlegame-plan & gem "show + explain the advantage" audit (2026-05-25)

**Trigger (David, verbatim):** *"How is this a gem?? Please audit all gems and
make sure they show and explain real advantages! This doesn't neither!! NOT
GOOD!!"* — screenshot of **"Dutch Leningrad: The Kingside Storm"** (Watch &
Learn, MOVE 4/4) showing a quiet middlegame position with no advantage.

**Scope chosen by David:** *Everything* — plan watch-lines AND re-verify gems.
**Target branch:** `claude/wlpp-practice-unlock-issue-5mhqu` (David's call;
merged origin/main in to get the real content — PR #673 diff stays net-only).

---

## Diagnosis (root cause, not symptom)

The thing David tapped is **NOT a mined punish-gem** — it's a **middlegame-plan
`playableLine`** (`mp-dutchdefence-main` in `src/data/middlegame-plans.json`)
rendered through the WLPP Watch player (`PlayableLinePlayer`). Its moves were
`d5 Na6 Rb1 Bd7` — **zero** kingside-storm moves — and the final annotation
just *promised* the attack: *"…Bd7 develops; Black is ready for …e5 and the
kingside storm…"*. So the line neither **showed** the theme (named after an
attack it never plays) nor **explained** a concrete advantage.

**The disease is systemic.** The plan `playableLines` were generated by walking
generic continuation plies from the critical position WITHOUT ensuring the moves
demonstrate the plan's own declared `pawnBreaks`. Measured by
`scripts/audit-plan-line-themes.mjs`:

- **141 plan watch-lines** across 121 openings.
- **65 are "theme-empty"** (play none of the plan's declared pawn breaks).
- **13 end on a bare promise** ("ready for…", "poised to…", "prepares…").
- **68 distinct offender lines** across 25 openings (after the Dutch-main fix).

Offenders by opening (post-fix): french-defence 10, nimzo-indian 8,
scandinavian 5, italian/alekhine/caro-kann 4 each, scotch/benko/dutch 3 each,
ruy/pirc/dragon/najdorf/sveshnikov/four-knights/english/reti 2 each, then 1 each
for qgd, kid, kings-gambit, vienna, alapin, london, catalan, kia.

**Gems are in much better shape** (the eval IS the shown advantage):
- `src/data/punish-gems.json`: **134 gems, all weapon-tier** (103 `confirmed`
  ≥ +1.0, 31 `positional` ≥ +0.5). Only `confirmed`/`positional` + hand-narrated
  gems surface (`isWeaponGem` ∧ `hasGemNarration`).
- The tile shows the eval ("Crush +1.8" / "Edge +0.7"), so a surfaced gem DOES
  show its advantage. **16 gems** have a `why` tile-text that's eval-templated
  but thin on the tactical reason (names neither a square nor a concrete gain).
- True eval RE-verification = re-run `scripts/mine-punish-gems.mjs` (engine +
  explorer) → **CI** (`.github/workflows/mine-punish-gems.yml`); the sandbox
  explorer allowlist is unreliable. Not done this session.

---

## GAPS CLOSED FOR FUTURE BUILDS (2026-05-25, David's directive)

Before authoring the gem/common-mistake backlog, locked the standard into gates
+ rules so the holes can't reopen. All three coverage gates are manifest-driven
(keyed off `opening-manifests.json`, so a new masterclass opening is auto-in-
scope) with SHRINKING baselines, and proven non-vacuous (fail on any new gap):

- **Plan lines — short register.** `middlegamePlanThemes.test.ts` now also
  requires `learnCues` on every masterclass plan line. Baseline
  `middlegamePlanShort.baseline.json` = **119** lines missing short (the 15
  rewritten this session have it). New masterclass plan lines must ship both
  registers.
- **Punish-gems — narration coverage.** `punishGems.test.ts` now requires a
  `GEM_NARRATION` entry for every masterclass gem. Baseline
  `punishGemNarration.baseline.json` = **42** mined-but-un-narrated gems
  (King's Gambit 20, Scotch 15, Italian 7). Un-narrated gems don't surface, but
  this gate makes the backlog visible and forces it down.
- **Common mistakes.** Already gated (`commonMistakeNarration.test.ts`,
  manifest-driven, full `explanation` + ≤8-word `shortNarration`). 37
  non-masterclass entries lack short — sanctioned fallback, not gated.

Rule corrections in CLAUDE.md: the move-dictation fallback is now explicitly
ONLY for non-masterclass / DB-only lines; masterclass curated content (plan
lines, gems, common mistakes) REQUIRES both registers, enforced by the gates.

### Backlog (the actual content authoring, next phase)
- [ ] 119 masterclass plan lines → hand-author short cues (+ quality-pass full).
- [ ] 42 masterclass gems → hand-narrate (priority: King's Gambit, Scotch, Italian).
- [ ] Quality-audit the 92 already-narrated gems (correct advantage, per subline).

## RESULT (full sweep, 2026-05-25)

The original "65 theme-empty" was inflated by two regex bugs in my own detector:
(1) object-form `pieceManeuvers` use a `route` field, not `move`, so their goal
squares were never extracted; (2) the square regex required a `\b` word boundary,
so piece-glued squares (`Ne7`, `Bd6`, `Nf5`) never matched. After fixing both and
adopting the defensible definition (a student move must land on an UNCONDITIONAL
break OR a declared maneuver square; conditional breaks like "…c5 only if" are not
required), the **true offender count was 18**, not 65.

**18 → 4.** Rewrote **14** lines (grounded, chess.js-verified via the harness):
7 promise-enders got concrete, non-promise narration (some extended one move to end
on the student's strong move — e.g. Nimzo Leningrad now ends on …g5, the storm; Dutch
Classical on …Ne4, the outpost); 7 theme-empty lines now play a real break/maneuver
(Najdorf …b5, Benko …e6, Nimzo-Kasparov …e5 + …Bxc3, Alekhine 4-Pawns …f6, Fort Knox
…Re8, Portuguese …e5, Réti Bf4). All carry full Watch narration + short Learn cues +
auto-generated lead-eye highlights.

**4 left flagged — deliberately, never fabricated.** `ruy-lopez exchange-endgame`,
`scotch-game gambit`, `alapin main`: their thematic squares are already occupied/passed
at the critical FEN (the theme happened *before* the position) — the lines are sound,
the gate proxy just can't confirm. `nimzo huebner`: in its FEN, …e4 appears to win a
piece, so the "positional wedge" narration isn't verified-sound. Per the cardinal rule
(when unsure: leave flagged, never invent) these stay in the baseline.

Tooling: `scripts/apply-plan-line-patches.mjs` (gate-safe authoring harness) +
`scripts/plan-line-patches.mjs` (the 14 authored patches) + `scripts/plan-line-dossier.mjs`.

## Done (initial)

1. **Fixed `mp-dutchdefence-main`** (David's exact line) to the masterclass bar.
   New line `d5 g5 b4 g4 Nd2 f4` — chess.js-verified; `…g4` genuinely forces the
   f3-knight to move (replies are natural, not arbitrary); ends ON the `…f4`
   break with the f-file cracking open. Concrete per-move narration (no promise),
   `learnCues`, and lead-the-eye arrows/highlights (orange move squares, green
   vision arrows, yellow named targets). No longer flagged.
2. **Built the gate** — `src/data/middlegamePlanThemes.test.ts` +
   `src/data/middlegamePlanThemes.baseline.json` (68 known offenders) +
   `scripts/audit-plan-line-themes.mjs`. Blocks NEW theme-empty / promise-only /
   illegal-FEN lines; guards the Dutch fix; baseline only SHRINKS. Wired into
   `ship-check` GATE_TESTS.

## Phased plan (remaining — grounded, never invented)

Each offending plan gets its `playableLine` rewritten to **play the plan's own
declared break** (DB/`chess.js`-validated from the critical FEN, natural/forced
replies), narrated from the plan's authored `pawnBreaks`/`pieceManeuvers` prose,
ending ON the break (the payoff), with lead-the-eye markers. Per opening:

- [ ] french-defence (10) · [ ] nimzo-indian (8) · [ ] scandinavian (5)
- [ ] italian (4) · [ ] alekhine (4) · [ ] caro-kann (4) · [ ] dutch (3 left)
- [ ] scotch (3) · [ ] benko (3) · then the 2s and 1s.
- [ ] Refine the gate's one known false-positive class: a line whose break
  already happened BEFORE its FEN (e.g. `mp-dutchdefence-leningrade5`, fen is
  post-…e5) is "theme-empty" by the heuristic but legitimately shows the
  aftermath — improve it to also accept a break in the FEN's move-history, or
  require the line to show a concrete consequence.
- [ ] Gems: prune the 16 thin `why` fields to name the tactic; schedule a CI
  miner pass to RE-verify all 134 evals against the live explorer + Google-spot-
  check the headline crushes (gem doctrine §6).

**Decisions log:** David chose full scope + this branch (2026-05-25). Fixes are
grounded — when a sound thematic line can't be verified, the line is left
flagged in the baseline, never fabricated (cardinal rule: empty > generic >
invented).

**Next-session pickup:** run `node scripts/audit-plan-line-themes.mjs` for the
live offender list; fix per opening; remove fixed keys from
`middlegamePlanThemes.baseline.json`; re-run the gate.
