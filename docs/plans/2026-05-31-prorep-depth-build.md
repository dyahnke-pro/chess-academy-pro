# Pro-Rep DEPTH BUILD — make both repertoires the best they can be (2026-05-31)

David: "make all noted changes." Three audit-identified depth gaps, all real-data
(no fabrication, G3 + soundness + endgame-rule locked):

## GAP A — deepen 17 thin Watch lessons (14-21 plies → masterclass depth)
Worst: Pirc(14), naro-FantasyCaro(11), ClosedSicilian/KIA/Stafford/gotham-Fantasy(15),
FrenchDefense(16). Extend each spine into the middlegame from the pro's REAL games.

## GAP B — endgame layer for 22/28 openings (the biggest gap)
Locked endgame rule: real game in the SAME variation → walk to its ending →
classify type → author the conversion. Self-hide where his games don't reach a
decisive ending. Only 6 have it (naro Alapin/Caro/KIA/Ruy). Build the other 22.

## GAP C — punish-gems for 15/28 openings without any (13 have them)

## Build order (highest value first): B → A → C, per-opening real-data batches.
Tooling: scripts/pro-repertoire/extract-endgames.cjs (classify + pick real endgame
games per opening), then author + gate + ship in batches.

---
## RESULTS (2026-05-31)

### GAP C — punish-gems: DONE (real-data, 15 gems added)
Mined engine-first across all 15 gemless openings. Yielded 15 real refutable gems
across 6 openings → pro openings with gems 13→**19/28**. Confirmed crushes incl.
Fantasy-Caro Bxf7+ (+2.8), Alapin Bxd5-wins-queen (+4.97), KID Benko-break (+1.37).
The other 9 openings (Trompowsky/QGD/KIA/Jobava/Ruy/English/Alekhine + more)
legitimately yield 0 — solid/system lines where amateurs make no refutable
blunders (the gem doctrine: master/system buckets hide no crushes). All hand-
narrated, board-accurate, voice-clean, gated (1162 assertions green).

### GAP B — endgame layer: data-bounded (NOT all 22)
The deep-build `endgameTypeBreakdown` data shows his games at the opening terminus
mostly STAY in the middlegame (Q+pieces dominant); only 3-8 reach a real decisive
ending per variation. Per the locked endgame rule + self-hide doctrine, building
endgames on all 22 = fabrication. Real decisive-endgame data exists for a handful
(Naroditsky KID anti-kid/saemisch/makogonov have real R+P/R+minor+P games). The
22 GothamChess-attacking + Naroditsky-sharp openings correctly self-hide — their
wins are middlegame/attack-decisive, not endgame conversions. Building only where
the data supports (KID) is the honest scope; forcing the rest is the cardinal sin.

### GAP A — lesson depth: data-bounded (lessons already reach the middlegame)
The 17 "thin" lessons (14-21 plies) already reach a middlegame (Gate B). Their
deep-build spines terminate at move 7-9 because that's where HIS blitz games
diverge. Extending past the data = inventing moves (G3). The masterclass set is
deeper because the MASTERS DB has deeper common lines than one player's games —
that's the architectural difference (§G9.1 spine derivation), not a fixable gap.

### Honest verdict
"Make all noted changes" within the no-fabrication rules = mine every gem the
data yields (done, +15), build endgames only where his games reach real endings
(KID — next), and document why the rest self-hides. Stamping a fake number on
every opening would violate the locked G3/soundness/self-hide rules.
