# Surface map — `src/services/tacticClassifierService.ts`

> GENERATED by `node scripts/surface-map.mjs`. Do not hand-edit — ship-check
> regenerates this and fails the push if it differs, which is how the map is
> proven FRESH rather than merely present. Read it before you change the file.

**491 lines · 13 exports · 4 importers · 2 tests · 0 audits**

## Locked rules that govern this surface

_No CLAUDE.md section names this file or its exports. That is itself worth knowing: nothing is written down, so the blast radius below is the only guide._

## Who calls in

- `src/components/Puzzles/PuzzleBoard.tsx`
- `src/services/gameAnalysisService.ts`
- `src/services/gameInsightsService.ts`
- `src/services/tacticalProfileService.ts`

## Exports and every call site

### `TACTIC_LABELS` (const) — 0 call sites
- _no call sites outside this file — unused, or reached only through a re-export_

### `LICHESS_THEME_TO_TACTIC` (const) — 0 call sites
- _no call sites outside this file — unused, or reached only through a re-export_

### `LICHESS_THEME_LABELS` (const) — 0 call sites
- _no call sites outside this file — unused, or reached only through a re-export_

### `getTacticTypeFromThemes` (function) — 1 call site
- `src/components/Puzzles/PuzzleBoard.tsx:134`

### `getPrimaryThemeLabel` (function) — 1 call site
- `src/components/Puzzles/PuzzleBoard.tsx:135`

### `classifyTacticsFromGame` (function) — 1 call site
- `src/services/gameAnalysisService.ts:1971`

### `deriveMissedTacticsForGame` (function) — 1 call site
- `src/services/gameInsightsService.ts:778`

### `backfillClassifiedTactics` (function) — 0 call sites
- _no call sites outside this file — unused, or reached only through a re-export_

### `getTacticMotifStats` (function) — 0 call sites
- _no call sites outside this file — unused, or reached only through a re-export_

### `getTacticsForGame` (function) — 0 call sites
- _no call sites outside this file — unused, or reached only through a re-export_

### `getClassifiedTacticCount` (function) — 0 call sites
- _no call sites outside this file — unused, or reached only through a re-export_

### `getRecentClassifiedTactics` (function) — 0 call sites
- _no call sites outside this file — unused, or reached only through a re-export_

### `getTacticsByType` (function) — 0 call sites
- _no call sites outside this file — unused, or reached only through a re-export_

## Tests

- `src/components/Puzzles/PuzzleBoard.test.tsx`
- `src/services/gameInsightsService.tacticsMissed.test.ts`

## Audits that reach it

_No audit script names this file or its exports. Runtime behaviour here is unproven._
