# Audit Index

Single manifest for every UI/surface audit in the app. New audits add a
row here when they ship. Open audits get a status flag so parallel
sessions don't double-drive the same surface.

The pattern across audits is consistent:
- **UX contract** doc — what the surface MUST do, hand-written contract
- **Audit script** — Playwright drive against the deployed app
  (`scripts/audit-<surface>.mjs`), green or fail per surface
- **E2E spec** — Vitest/Playwright spec under `e2e/` for build-time
  regression
- **Audit reports** — `audit-reports/<surface>-<iso>/` (gitignored,
  generated per run)

Status legend: ✅ shipped · 🟡 partial · 🚧 in flight (another session) ·
❌ untouched · — N/A on this surface.

---

## Coverage matrix

| Surface | Status | UX contract | Audit script | E2E spec | Notes |
|---|---|---|---|---|---|
| The Academy — course surfaces (shelves / syllabus / Spar trainer) | ✅ | 3 shelves render (Masterclasses/Counter-Weapons/Gambits); syllabus = chapters + ranked sublines + Resume + Spar; trainer mounts (board + prompt, no crash); zero page errors | [`scripts/audit-academy-courses.mjs`](../scripts/audit-academy-courses.mjs) | `openingCourse.test.ts`, `courseTrainer.test.ts`, `courseEntitlement.test.ts`, `courseWhyFacts.test.ts`, `Academy/*.test.tsx` | 2026-06-16. Deterministic course feature (no LLM) — verifies render + interaction. 8/8 green vs localhost. When narration (Stage-2) lands it routes through the coach grounding architecture and inherits its claim validators + audit-stream; extend this audit with the narration-listener + audit-stream instruments then. |
| Gambit tab — full walk (data) | ✅ | — | [`scripts/audit-gambit-tab-walk.mts`](../scripts/audit-gambit-tab-walk.mts) | content gates (lessonIntegrity/narrationAccuracy/narrationGrounding/lessonDepth) | 2026-05-31. Personally walks all 12 gambit-tab openings + every variation lesson from the start: legality, ply length, middlegame reach, full+short narration, board-claim accuracy, arrow legality, tab→lesson coverage, opening→middlegame→endgame continuity, plans/model-games/pitfalls. Run `npx tsx`. Surfaced that the 7 gambits.json lessons rendered curated Watch lessons while escaping every content gate — 6 now registered + gated. |
| Gambit tab — interactive (runtime) | ✅ | Watch=curated LessonPlayer (never legacy WalkthroughMode); tabs resolve; voice fires | [`scripts/audit-gambit-tab-interactive.mjs`](../scripts/audit-gambit-tab-interactive.mjs) | — | 2026-05-31. Drives the live UI through every gambit + every variation tab: detail mounts, tabs render, Watch mounts `[data-testid=lesson-player]` (NOT `walkthrough-mode`), beat depth read, `/api/tts` fired. 94/94 PASS (0 legacy fallbacks across ~45 lessons), 6 headless tts-timing warns. Runs vs localhost (this session's fixes) or prod via `AUDIT_SMOKE_URL`. |
| Settings | ✅ | — | — | — | PR #502, #503 + Coach Narration unification (2026-05) |
| First-run strength calibration (boot + Dashboard bubble) | ✅ | — | [`scripts/audit-strength-calibration.mjs`](../scripts/audit-strength-calibration.mjs) | `strengthCalibrationService.test.ts`, `StrengthCalibrationBubble.test.tsx`, `PageHelp.test.tsx` | 2026-05-26. Makes difficulty adaptive for beginners. Imported games seed strength silently at boot (`calibrateStrength`); with no imports the skill-band bubble pops first (PageHelp-style, blocking) and seeds BOTH `currentRating` + `puzzleRating` (they used to diverge — engine read one, puzzles the other). 4/4 sandbox scenarios green: bubble-first + help-suppressed, both-ratings-written, help-pops-second, sticky-no-repop. Live-commit slow in sandbox (IDB write-stall) — unit tests prove the write logic. |
| AI data-sharing consent persistence (first-run + Settings) | ✅ | — | [`scripts/audit-ai-consent-persist.mjs`](../scripts/audit-ai-consent-persist.mjs) | `AiConsentModal.test.tsx`, `aiConsentStore.test.ts` | 2026-07-03. Fix `b2707d5`: consent modal + Settings toggle wrote the decision to in-memory Zustand only (`setActiveProfile`), never Dexie, so a full app close re-prompted on every cold boot. Both paths now also `updateProfile(id, { aiDataConsent })`. Audit drives a fresh install → Allow/Don't-allow → reload (cold-boot sim: fresh heap + Dexie intact) → asserts no re-pop. 5/5 green on localhost; prod Chromium nav ERR_CONNECTION_RESET in this container (egress quirk) so ran against dev server per the G1 fallback — true cold-boot confirmation is on-device (TestFlight build 94). |
| Learn-with-Coach (`/coach/teach`) | ✅ | — | — | — | PR #499 + Phase 1+2 narration overhaul. Locked at tag `learn-stable-2026-05-08`. |
| Grounded training/weakness recommendation (`/coach/teach` meta-questions) | ✅ | [`docs/plans/2026-07-04-grounded-weakness-recommendation.md`](plans/2026-07-04-grounded-weakness-recommendation.md) | [`scripts/audit-coach-training-recommendation.mjs`](../scripts/audit-coach-training-recommendation.mjs) | `questionIntents.test.ts`, `groundedAnswer.test.ts`, `trainingAidRouter.test.ts` | 2026-07-04 (PR #770). "What should I train / learn next / what tactics am I weak in / where am I losing" now get a GROUNDED conversational reply — the LLM voices a weakness profile computed in code (`assembleWeaknessRecommendation` ← `getUnifiedWeaknessProfile`) via `voiceFacts`, never inventing (G0). Was: dead-ended at the opening picker / hijacked into a drill. Seeds a synthetic `classifiedTactics` weakness profile, drives the 6 meta-questions vs the LIVE prod brain (proxy-intercept), asserts each fires `intent=grounded_voice` (audit-stream) + voices the computed facts. **6/6 grounded, 0 fail on prod** (bundle `index-D-jyvY99`). |
| Coach grounding COVERAGE (fall-through rate) | 🚧 | [`docs/plans/2026-07-09-coach-grounding-unification.md`](plans/2026-07-09-coach-grounding-unification.md) | [`scripts/audit-coach-grounding-coverage.mjs`](../scripts/audit-coach-grounding-coverage.mjs) | `coachInversion.gate.test.ts`, `voiceFactsFidelity.test.ts` | 2026-07-09 (item #7 instrument). MEASURES coverage (not a break-it loop): drives a coverage battery through `/coach/teach`, captures every `coach-llm-call` audit event via the narration-listener sidecar (`coach-llm-call` is audit-stream-only, NOT in PostHog's `AUDIT_EVENT_MAP`), and reports grounded=true vs grounded=false per intent + the exact ungrounded questions to close. Exit 1 on any fall-through so a wrapper can gate. Runs against a local dev server built from the branch (measures the NEW build pre-ship) or prod post-deploy. Drives the fall-through rate toward ~0 (the item-#7 ship gate). |
| Coach diagnosis-vs-drill-vs-teach ADVERSARIAL boundary (`/coach/teach`) | ✅ | — | [`scripts/audit-coach-weakness-adversarial.mjs`](../scripts/audit-coach-weakness-adversarial.mjs) | `questionIntents.test.ts` | 2026-07-04 (PR #772). Adversarial "break it" audit of the classification boundary — the same word ("tactics"/"opening"/"weak") must route differently by phrasing: DIAGNOSIS→grounded weakness reply, IMPERATIVE→drill, TEACH→opening, CONCEPT→definition, SINGLE-GAME→not an aggregate dump. Hammers it with typos/British/emoji/multi-intent/contradiction/state-chaos vs the LIVE prod brain, classifies by reply CONTENT (voiceFacts is the shared G0 chokepoint so the stream can't tell progress from concept). **Found + fixed 4 real misroutes**: "am i improving" / "I keep hanging my queen" hijacked to a fuzzy "did you mean <opening>?" picker; "what's my worst opening" → teach picker; "i suck at endgames" → board best-move. Fix: `handleSubmit` skips the fuzzy opening matcher for pure `isProgressQuestion`/`isConceptQuestion` asks; `isProgressQuestion` gained informal predicates ("suck/stink at") + opening-weakness scoping. Post-fix sweep: 0 real misroutes on prod (`index-DUcywArq`). |
| Endgame (`/coach/endgame`) | ✅ | [`docs/endgame-ux-contract.md`](endgame-ux-contract.md) | — | `e2e/coach-endgame.spec.ts` | PR #500. 24 specs passing, 2 skipped. |
| Opening Traps (`/openings/traps`) | ✅ | — | — | — | PR #494 + classification taxonomy (`trap` / `mistake` / `theme`). |
| Masterclass variation Watch (rebuilt-spine render+voice) | ✅ | — | [`scripts/audit-masterclass-variation-watch-prod.mjs`](../scripts/audit-masterclass-variation-watch-prod.mjs) | — | 2026-05-30. Scoped, fast post-deploy check for variation tabs whose LessonScript spine was rebuilt (the heavy `audit-punish-gems-loop` times out vs slow prod). Asserts the variation Watch mounts the CURATED LessonPlayer (positive `[data-testid=lesson-player]`, NOT legacy `walkthrough-progress`) + fires a real beat `/api/tts` (warmup `.` probe excluded). Enables Polly on the seeded profile (fresh-context default is off). Targets edited in the `TARGETS` array. 6/6 green vs prod for pirc-150 + two-knights Max Lange. |
| Masterclass endgame card + per-variation model games | ✅ | — | [`scripts/audit-masterclass-endgame-modelgames-prod.mjs`](../scripts/audit-masterclass-endgame-modelgames-prod.mjs) | — | 2026-05-30. Post-deploy render audit for the Caro Classical endgame card (`plan-line-mp-carokann-main-endgame` renders + its Watch fires a beat `/api/tts`) and per-variation model-game scoping (main tab shows the full library; a variation tab narrows to its tagged games). 5/5 green vs prod (Caro endgame speaks 4 beats; Advance tab 1 game vs main 7). |
| Trap orientation (`repertoire.json` + `pro-repertoires.json`) | 🟡 | [`docs/plans/2026-05-16-trap-orientation.md`](plans/2026-05-16-trap-orientation.md) | [`scripts/audit-trap-orientation.mjs`](../scripts/audit-trap-orientation.mjs) (pro-repertoires) + [`scripts/audit-repertoire-orientation.mjs`](../scripts/audit-repertoire-orientation.mjs) (repertoire) | `src/data/pro-repertoires-orientation.test.ts` + `src/data/repertoire-orientation.test.ts` | 2026-05-16. Phase 1 gate landed: build-time test with allowlist baseline of 166 known repertoire.json offenders. 190/432 total flagged (2 INVERTED_MATE, 37 PGN_NOT_IN_DB, 26 INVERTED_MATERIAL). Phases 2-4 shrink the allowlist; Phase 5 deletes it. |
| Coach Review (`/coach/review`) | ✅ | [`docs/review-with-coach-ux-contract.md`](review-with-coach-ux-contract.md) | [`scripts/audit-coach-review.mjs`](../scripts/audit-coach-review.mjs) | `e2e/coach-review.spec.ts` | PR #496, #501 + Wave 4 (2026-05-14). 23/23 specs green. +2026-06-28: grounded board previews (`review-citation-previews` + `-why-` line + tap-to-jump) scenario added. |
| Tactics (`/tactics/*`) | ✅ | — | [`scripts/audit-tactics.mjs`](../scripts/audit-tactics.mjs) | — | 33 scenarios, 111 checks. 8 bugs fixed during audit. |
| Play-with-Coach (`/coach/play`) | ✅ | — | [`scripts/audit-coach-play.mjs`](../scripts/audit-coach-play.mjs) | — | 17 unit tests for resolvers. `narrationDensity` reads `resolveLlmNarrationDensity`. |
| Play-with-Coach — RESUME (`/coach/play` persistence, WO-RESUME-01) | ✅ | [`docs/plans/2026-06-09-coach-play-resume.md`](plans/2026-06-09-coach-play-resume.md) | [`scripts/audit-coach-play-resume.mjs`](../scripts/audit-coach-play-resume.mjs) | `useChessGame.test.ts` (loadHistory) + `coachPlayPersistence.test.ts` (v2 round-trip) | 2026-06-09. The game now PERSISTS across leaving + re-entering the Play tab (David: "if you leave the tab and then reenter you lose your game"). Re-enabled resume by persisting the **SAN history + game-state + clock** (v2) and rebuilding via `loadHistory` so there are no "ghost squares" (the FEN-only restore that got it disabled). Audit: play moves → leave → return → move list restored + snapshot SAN intact; resign clears it. |
| Read-this-position (`/coach/play` narration button) | ✅ | — | [`scripts/audit-read-position-prod.mjs`](../scripts/audit-read-position-prod.mjs) | `usePositionNarration.test.ts`, `PositionNarrationBanner.test.tsx` | 2026-06-12. Beta-tester report: the read button "didn't narrate" + the subtitle "disappeared when he scrolled." Two fixes: (1) banner is now `position:sticky` so it stays pinned during scroll; (2) the explicit read routes through `speakReadAloud` (bypassVerbosity) instead of `speakForced`, so it ALWAYS speaks even on Coach Narration = silent/brief (it was a dead control on those settings — third sanctioned read-aloud carve-out, CLAUDE.md G5). Probe forces `coachNarration='silent'`, taps the button, asserts the banner appears + is sticky + a `/api/tts` request fires (voice path engaged despite silent). **6/6 green on prod.** Caveat flagged to David: headless can't reproduce the iOS-standalone-PWA web-speech-after-async-gap block, so on-device audibility still needs an iPhone check. |
| In-game Hint (`/coach/play` Hint button) | ✅ | — | [`scripts/audit-hint.mjs`](../scripts/audit-hint.mjs) | `useHintSystem.test.ts`, `HintButton` via `CoachGamePage.test.tsx` / `PracticeMode.test.tsx` | 2026-06-01. Verifies the relabel (button reads "Hint", not "Show Answer" — also a deploy check) + Tier-3 verbosity cap (MAX 2 sentences / 40 words; flags the 650-char/6-sentence regression) + anti-invented-tactic grounding (scans the spoken/rendered hint for ungrounded pin/fork/skewer claims). Captures `/api/tts` text + hint audit events. |
| Play-with-Coach — FULL interactive E2E (`/coach/play`) | ✅ | — | [`scripts/audit-coach-play-full.mjs`](../scripts/audit-coach-play-full.mjs) | `coachGamePersistence` + `mistakePuzzleService` / `misconceptionService` / `autoAnalyzeGame` (38 tests) | 2026-06-01. 3-instrument (Playwright + prod audit-stream + narration listener). Drives David's full ask: coach plays the Italian AGAINST you (opening-lock correctness, all moves legal + auto-detected C50), hint button, ask-a-question, off-book warning, **blunder-interception**, end-of-game review, ask-about-mistakes, mistake logging. **16/16 green on the deployed build.** Found + FIXED: `handleResign` bypassed `finalizeGame` → resigned games were never saved/auto-analysed; now routed through `finalizeGame('loss','resign')` with a shared `MIN_PERSIST_PLIES` floor (`src/utils/coachGamePersistence.ts`). That also resolved the "logs nothing" symptom — the resigned game's blunder now auto-logs as a `mistakePuzzle`. |
| Coach Chat (`/coach/chat`) | ✅ | [`docs/coach-chat-ux-contract.md`](coach-chat-ux-contract.md) | [`scripts/audit-coach-chat.mjs`](../scripts/audit-coach-chat.mjs) | `src/components/Coach/CoachChatPage.test.tsx` | 2026-05-14. Memory-mirror bug fixed (fast-paths now write to `useCoachMemoryStore`). 15/15 prod audit + 18/18 unit tests. |
| Coach Chat — sister surfaces (memory-mirror class sweep) | ✅ | [`docs/coach-chat-ux-contract.md` §Bug Log 2026-05-14 (sister surfaces)](coach-chat-ux-contract.md) | reuses `audit-coach-chat.mjs` pattern; per-surface drives pending | each surface's own `.test.tsx` | 2026-05-14. Same class fixed across `GameChatPanel.tsx` (10 sites), `VoiceChatMic.tsx` (4), `CoachTeachPage.tsx` (5), `CoachGamePage.tsx` (2). 21 total. |
| Coach Teach — unknown / sub-line resolution (`/coach/teach`) | ✅ | — | [`scripts/audit-coach-teach-unknown-line.mjs`](../scripts/audit-coach-teach-unknown-line.mjs) | `openingDetectionService.test.ts` (spine-extension invariant) | 2026-05-18. Drives "teach me [obscure sub-variation]" through the surface: (A) a real DB sub-line not in the static registry — verifies DB-narration spine extends to middlegame via cross-name continuation, leaf surfaces "Play this line out yourself", coach asks via chat; (B) a fabricated name — verifies pre-flight reject + brain fall-through. Spine-extension fix: `findShortestCanonicalPgn` now follows PGN-prefix continuations across name boundaries when the same-name spine is < 10 plies AND no fork branches exist (Frankenstein-Dracula 6→20 plies). Leaf prompt: chat message + voice + prominent button asking the student to play the line out at end of walkthrough. Also covers 5-mode e2e walks (Teach / Drill / Quiz / Trap / Play) for Vienna (static registry) + Caro-Kann Classical (Tier 3 DB-narration). |
| Coach Teach — in-page middlegame plans + play-out (`/coach/teach`) | ✅ | — | [`scripts/audit-coach-teach-middlegame-plans.mjs`](../scripts/audit-coach-teach-middlegame-plans.mjs) | `middlegamePlanner.test.ts` (distinctive-token resolution) + `coachAgent.test.ts` (midgame intent) | 2026-05-29. Guards the fix for "Learn-with-Coach can't teach Pirc middlegame plans" (it fuzzy-matched "Pirc" → Evans Gambit "Pierce Defense"). Drives: "middle game plans in the Pirc" → in-page picker of all 8 authored Pirc plans (no fuzzy garbage, no "no exact match" brush-off); tap a plan → in-page runner mounts + voice fires (3 instruments); at plan end → "play it out?" picker → in-page `OpeningPlayMode` from the plan's start position WITHOUT leaving the tab; off-canonical "midgame plans caro kann" → 7 Caro plans; "Latvian Gambit" (no plan) → honest no-plan message, never a wrong opening. 16/16 in sandbox. |
| Coach Discovery — rotating greeting + grounded pickers (`/coach/teach`) | ✅ | — | [`scripts/audit-coach-discovery-prod.mjs`](../scripts/audit-coach-discovery-prod.mjs) | `coachGreetings.test.ts` (rotation + grounded-routing) | 2026-07-04. Client-side post-deploy smoke (no provider key): navigates `/coach/teach` on prod via the prod-bridge, asserts the Learn kickoff renders a ROTATING greeting from `COACH_GREETINGS` (not the old static "welcome to my classroom" line), the grounded suggested-question chips render (`coach-choice-chip-0` carries a real question), and zero console/page errors on mount. Verified 5/5 on prod bundle `index-B1twRxOl` after the #779 merge. NB: the grounded ANSWERS + the voiceFacts number/SAN fidelity net need a provider key + seeded game data (the `.fixtures/david-games.json` export) — that's the keyed on-device step, not covered here. |
| Coach Tactical-Awareness wiring (cross-surface) | ✅ | — | [`scripts/audit-coach-tactical-awareness.mjs`](../scripts/audit-coach-tactical-awareness.mjs) | `liveTacticsContext.test.ts` (8 unit tests) | 2026-05-18 (WO-COACH-TACTICAL-AWARENESS phases 1-3). Verifies the new `TacticsLiveContext` block fires + the rating-adaptive lookahead lands in {1, 2, 4, 6}. Pulls the captured coach reply text from the audit-stream `coach-memory-conversation-appended` events when an LLM key is configured (in the sandbox, the brain reaches Anthropic and the key validates — narration text needs a funded account or a prod run to verify). Soft-checks /coach/play page mount + live-game pipeline. Tactical-context block is wired into 6 brain-call sites: /coach/teach (handleSubmit), /coach/play (move-selector + explore-chat + blunder-alert), /coach/review (ask), /coach/play in-game chat (GameChatPanel), /coach/analyse (initial explain + follow-up ask), /coach/session/explain-position (initial + ask). |
| Coach Middlegame + Endgame mode coverage | ✅ | — | [`scripts/audit-coach-middlegame-endgame.mjs`](../scripts/audit-coach-middlegame-endgame.mjs) | — | 2026-05-18. Probes both surfaces against the 5-mode template (Teach / Drill / Quiz / Trap / Play) and emits a coverage matrix. Endgame `/coach/endgame` covers Teach + Drill (+ Play in eval-lab and the lesson tabs); no Trap (endgames don't model opponent traps the same way openings do). Middlegame `/coach/session/middlegame` is Teach-only via `useWalkthroughRunner` — Drill / Quiz / Trap / Play are MISSING and deferred. 23/23 assertions; the gaps are reported, not patched (audit-only scope decision 2026-05-18). |
| Weaknesses (`/weaknesses`) | ✅ | — | [`scripts/audit-weaknesses.mjs`](../scripts/audit-weaknesses.mjs) | — | PR #508 + deep audit 2026-05-14. 20/20 prod scenarios: 5 tab buttons render, switch-to-tab mounts correct content, mistake-row → review with `state.from='/weaknesses'`, back-from-review restores Mistakes tab (not Overview), top-bar back-btn exits cleanly. Seeds synthetic mistake puzzle to exercise row contracts on fresh prod context. |
| Openings explorer (`/openings`) | 🚧 | — | — | — | In flight on another session (artefacts at `audit-reports/openings-ui-*`). Coordinate before driving. |
| Opening walkthrough narration (`/openings/:id`, `/openings/pro/:player/:id`) | ✅ | — | [`scripts/audit-fantasy-caro-walkthrough.mjs`](../scripts/audit-fantasy-caro-walkthrough.mjs) | `WalkthroughMode.test.tsx`, `AnnotationCard.test.tsx`, `openingNarrationService.test.ts` | 2026-05-17. Drives the live walkthrough surface end-to-end (boots app, navigates to a pro repertoire, clicks Learn, polls per-ply narration). FAIL patterns: `Continuing this line:`, `Nxe4` recommended, "Italian Game" cross-family leak. PASS patterns: f3 / fxe4 / f-file / Fantasy / Bc4-on-a2g8 / Bronstein / Maroczy. Catches the disease class fixed by commits `1aab2e39` (curator context → LLM narrator), `67275276` (kill FEN-seed override + low-prefix-match null), `8be18a13` (AnnotationCard reads `narration` field). Currently scoped to Fantasy Caro (`pro-gothamchess-fantasy-caro`); generalise to a matrix of pro repertoires when next pass lands. |
| Opening deep walkthrough — every subline (`/openings/*`) | ✅ | — | [`scripts/audit-openings-deep-walkthrough.mjs`](../scripts/audit-openings-deep-walkthrough.mjs) | (data-driven; no per-test) | 2026-05-18 (PR #594). Sits down and plays through every opening's sublines (main + variations + trapLines + warningLines) move-by-move via Playwright. Drives off `pro-repertoires.json` (82 openings) and `repertoire.json` (40 openings), ~1060 sublines, ~1-2 hours. Per ply: captures annotation-card label + text + card-empty state, classifies against six heuristics: `card-empty`, `text-empty`, `continuing-this-line`, `generic-templated`, `color-mismatch` (the David-screenshotted bug class fixed in PR #593), `san-not-mentioned`. Resumable via `AUDIT_RESUME=1`; errored sublines retried on resume. Scope env: `AUDIT_SCOPE=pro\|repertoire\|all`. |
| Middlegame-plan lead-the-eye (`/openings/:id` Watch player) | ✅ | — | [`scripts/audit-leadeye-plans.mjs`](../scripts/audit-leadeye-plans.mjs) | `middlegamePlanner.test.ts` (lead-the-eye legality + grounding gate) | 2026-05-21. Interactive probe of the lead-the-eye treatment on middlegame-plan playable lines (David's NON-NEGOTIABLE: arrows + highlights point the eye where the narration is talking). Drives a white opening (Ruy) and a black one (Pirc): waits for Dexie plan-seed, reloads warm, opens a Watch player with a playable line, confirms the demo mounts + annotation renders + highlights actually paint (react-chessboard v5 puts `squareStyles` on an inner child div, NOT `[data-square]`) + arrows render + Practice phase reachable. 14/14 checks green. The unit gate proves the DATA (legal sight-lines + every highlight/vision endpoint named in the annotation); this proves the RENDER paints. |
| Masterclass WLPP + punish-gems post-deploy contract (`/openings/:id`) | ✅ | — | [`scripts/audit-punish-gems-loop.mjs`](../scripts/audit-punish-gems-loop.mjs) | `punishGems.test.ts`, `wlppNarration.test.ts`, `lessonTabIntegrity.test.ts` | 2026-05-24. **3-PASS CONTRACT (David): MET only on 3 CONSECUTIVE error-free passes; EACH pass touches EVERY function, each pass digs DEEPER than the last; any error resets the streak.** Scope = ALL masterclass openings (`AUDIT_OPENING=<id>` runs one in isolation; run the four in parallel). Each pass: (1) every gem-bearing opening's card + 4 WLPP buttons + **each gem tile names its real inaccuracy+punish** (correctness, not just presence); (2) every masterclass opening × every variation tab — WLPP buttons wired, and (pass ≥2) the variation **Watch lesson mounts AND is DISTINCT** (lesson titles must differ — catches a tab loading the wrong/duplicate lesson, e.g. the old main==Classical dup); (3) Watch mounts + narrates PROSE on the wire, (pass ≥3) full playout to completion; (4) Learn = MOVE/CUE only, Practice = SILENT — decoded off `/api/tts` (warmup `.` probe excluded); (5) Play leaves to the coach room; (6) named-trap Learn mounts; (7) no EMPTY gem card; (8) zero pageerrors/console errors. Deeper passes add cold-IDB, pick-before-load, out-of-order toggling, wrong-move-in-Learn, silence-under-interaction, and widen the deep checks to every weapon opening. Unit gates prove the DATA (legality + DB-anchor + narration alignment + the dup-line gate); the loop proves it RENDERS CORRECTLY + behaves on the wire. |
| Hikaru pro-rep interactive (`/openings/pro/hikaru/:id`) | ✅ | [`docs/plans/2026-05-31-hikaru-build.md`](plans/2026-05-31-hikaru-build.md) | [`scripts/audit-hikaru-prorep-prod.mjs`](../scripts/audit-hikaru-prorep-prod.mjs) | `proRepLessonCoverage.test.ts`, `variationMiddlegameDepth.test.ts`, `punishGems.test.ts`, `proRepNarrationVoice.test.ts` | 2026-05-31. INTERACTIVE 3-instrument post-deploy audit for the 5 Hikaru openings. Per opening: **Gate A** (Watch uses the curated masterclass LessonPlayer, NOT legacy `WalkthroughMode` — detects `[data-testid="walkthrough-progress"]`), **Gate B** (Watch reaches a middlegame), **gem recognition** (the 6 session gems surface — Caro ×5 + Closed-Sicilian ×1 via the `weapons-locked-card` signal, since the weapon section is unlock-gated and the IDB unlock-write stalls in-sandbox per G1.4; Pirc/Réti/Nimzo-Larsen correctly empty), variation-tab switching (pick-before-load + out-of-order). Three instruments: (1) Playwright drives the UI; (2) audit-stream pulled pre/post so the delta isolates the run; (3) `/api/tts` request capture as the voice signal. 22 pass / 0 fail. **Headless-voice caveat (G7):** TTS fires on prod (1 req/run on the curated lesson) but flakily in headless (audio autoplay needs a real gesture) — route a real-device voice confirm to David. |
| Carlsen plans — endgame + per-variation middlegame (`/openings/pro/carlsen/:id`) | ✅ | — | [`scripts/audit-carlsen-plans.mjs`](../scripts/audit-carlsen-plans.mjs) | `middlegamePlanThemes.test.ts`, `middlegamePlanner.test.ts`, `proRepTabPlanCoverage.test.ts`, `EndgamePlansSection.test.tsx` | 2026-06-01. Post-deploy (G1) for the 5 Carlsen endgame plans (Scandinavian / Caro-Kann / Modern / Closed-Sicilian / Réti — each grounded in a real Magnus game walked to the ending) + the 9 data-grounded per-variation middlegame plans (his most-played continuation from each variation terminus across the 24,683-game corpus). Per opening: asserts `endgame-plans-section` RENDERS on the main tab (not the `-empty` fallback); per variation: clicks the variation tab + asserts `middlegame-plans-section` renders. 14/14 green on prod (bundle `index-CtMQvBYJ`). King's Gambit correctly has NO endgame plan (sharp gambit → self-hides). Sparse-data variations correctly stay plan-less (empty > invented). |
| WLPP expert-pass unlock budget (`/openings/:id`) | 🟡 | — | [`scripts/audit-wlpp-unlock-budget.mjs`](../scripts/audit-wlpp-unlock-budget.mjs) | `wlppLadder.test.ts` (`unlockBudgetFor`), `openingService.ladder.test.ts` (`unlockOpeningAllLines` write) | 2026-05-24. David: "Give user only 2 weapon unlocks. One for each color." The "I already know this" escape is now a lifetime budget of ONE pass per color — spending it unlocks the WHOLE opening (every line), and a different opening of that color shows the pass spent (no live button). Two-tap confirm guards the misclick. **🟡 SANDBOX CAVEAT:** openings-store WRITES stall in the Claude Code sandbox (raw `indexedDB.open` times out too — env, not feature; reproduced with the feature stashed), so the commit/persistence/cross-opening scenarios FAIL in-sandbox. The write is unit-proven against fake-indexeddb; run this on a real device / prod to confirm the commit path. In-sandbox the meaningful green checks are the budget DISPLAY + two-tap ARM. |
| Per-opening interactive walk (`/openings/:id`) | ✅ | — | [`scripts/audit-opening-walkthrough.mjs`](../scripts/audit-opening-walkthrough.mjs) | `openingWiring.test.ts`, `wlppNarration.test.ts` | 2026-05-24. Parameterized walk of ANY masterclass opening (`AUDIT_OPENING=<id>`, default vienna-game) — generalized from the Vienna walk. Captures per-tab evidence: tab labels, gem tiles + tier + order, model game, key ideas, distinct lesson titles, and decoded `/api/tts` (Watch prose vs ≤8-word Learn cue). Clone target retired — use this with `AUDIT_OPENING` instead of copying the Vienna script. Pairs with `scaffold-opening.mjs` (stubs a new opening's lesson/variations/tab-plan files + prints the §0.7 wiring checklist). |
| Book readers — read-aloud verbosity bypass (`/openings/:id` Understand zone) | ✅ | — | [`scripts/audit-book-reader-prod.mjs`](../scripts/audit-book-reader-prod.mjs) | (covered by the voiceService read-aloud contract) | 2026-06-11. 3-instrument post-deploy for the From-the-Books / Overview / Key-Ideas / Classic-Wisdom readers. Guards the fix where `useProseReader` routed read-aloud through `speakForced` (honored the brief/silent cap → clipped passages) instead of `speakReadAloud` (`bypassVerbosity`). (1) Playwright taps a From-the-Books paragraph + Overview paragraph + Classic Wisdom header; (2) prod audit-stream pulled pre/post (endpoint health); (3) narration-listener sidecar asserts every read's `voice-speak-invoked` source is `voiceService.speakReadAloud` (0 `speakForced`, 0 `briefCap`) and the spoken text is the FULL passage (len≫30 words). Also nails the "can't tap paragraphs" gotcha — the first-visit "How to use a Masterclass" page-help modal (z-100) intercepts taps until closed. 9/9 green on prod (bundle `index-BNOEI2IT`). |
| Ruy named traps — Watch/Learn/Practice/Play (`/openings/ruy-lopez`) | ✅ | — | [`scripts/audit-named-traps.mjs`](../scripts/audit-named-traps.mjs) | `lessonIntegrity.test.ts` (narration-survives-the-transition gate) | 2026-05-21. The hand-authored named traps (weapons + "watch out for") gained the full WLPP row. Drives Noah's Ark (widened to main + closed tabs) + Tarrasch (Open tab): tile renders on its correct variation tab, all 4 buttons present, Watch mounts the beat lesson with narration intact, Learn mounts the guided player (voice + lead-the-eye), Practice mounts silent with a working Hint button (reveals the move arrow on tap), Play mounts the opening-locked coach board. 30/30 green, 0 pageerrors. The unit gate proves each lesson's beat `say` text carries onto the Learn/Practice line VERBATIM (David: "make sure the narration survives the transition"). |
| Money loop (weakness bucket) — end-to-end + linkage | ✅ | — | [`scripts/audit-money-loop.mjs`](../scripts/audit-money-loop.mjs) | `misconceptionService.test.ts`, `trainingPlanSelector.test.ts`, `MisconceptionsTab` via the loop | 2026-05-21. Verifies the money build as ONE system: STEP 0 narrow-path (no favourite → Training Plan **hard-stops** with a "Go to Openings" CTA) → STEP 1 favourite Ruy+Pirc → seed the bucket (the 3 faucets' output) → MIRROR (/weaknesses "Thinking Errors") renders rows + due/resting (SRS never-graduates) + Drill link → LINKAGE mirror→/coach/plan + same bucket feeds Mirror AND Hub → HUB Today's reps surfaces the weakness reps → faucet host (/coach/play) mounts. 18/18, 0 pageerrors. G7: the LLM slip→tag CLASSIFY is device-only (seeds synthetic tags = the faucet output); the classifier/orchestrator are unit-tested. Caught two real fixes: the narrow-path hard-stop, and the Thinking-Errors mirror being gated behind GameInsightsPage's "Analysing your games…" (now reachable independent of game analysis). |
| Coach Analyse (`/coach/analyse`) | 🟡 | — | [`scripts/audit-untouched-surfaces.mjs`](../scripts/audit-untouched-surfaces.mjs) | `CoachAnalysePage.test.tsx` | 2026-05-14. Smoke pass — FEN input + Load button visible, no pageerrors on mount. Deep flow (paste FEN, get explanation streamed) still pending. |
| Training Plan rolodex (`/coach/plan`) | ✅ | — | [`scripts/audit-coach-plan.mjs`](../scripts/audit-coach-plan.mjs) | `TrainingPlanRolodexPage.test.tsx`, `RolodexCard.test.tsx`, `RolodexCardStack.test.tsx`, `RolodexRow.test.tsx` | 2026-05-17 (WO-ROLODEX-UI-01 PR-2/3/4 rewrite). Full audit: hub-tile → /coach/plan nav, empty state with Browse Openings CTA per color, single-favorite render with 8 training rows, multi-favorite stack with active card body + collapsed back-card tabs, tab activation swaps active card + fires `coach-memory-rolodex-active-card-set` audit, mount-time reconcile fires `coach-memory-rolodex-order-set`, mobile manila-folder default reads `lastActiveRolodexColor`, folder switch updates aria-selected, row tap navigates to filtered destination. Seeds favorites via raw IndexedDB. Drag-reorder gesture NOT in the Playwright matrix — flaky against dnd-kit's 250ms activation delay + browser PointerEvent timing; covered by unit tests at the drag-end callback contract. |
| Coach × Master-Play grounding (cross-surface) | 🟡 | [`docs/plans/2026-05-16-coach-master-integration.md`](plans/2026-05-16-coach-master-integration.md) | [`scripts/audit-coach-master-integration.mjs`](../scripts/audit-coach-master-integration.mjs) | `coachApi.master-integration.test.ts`, `masterPlayCache.test.ts`, `masterPlayLookup.test.ts`, `masterPlayWatcher.test.ts`, `claimValidator.test.ts` | 2026-05-16 v1 (WO-COACH-MASTER-INTEGRATION). Runtime instrument of CLAUDE.md G3 — four cooperating layers (watcher prefetch, pre-injection, claim validator, retry/stock fallback) gate every move-question chat turn so the coach can't invent SANs / frequencies / player names / years. 80 vitest tests green; 7-scenario Playwright audit drives services through `page.evaluate` with fetch-mock. Surface wiring through `coachService` → spine + Layer C (LLM tool-use loop) deferred to follow-up PR. |
| Coach × Player-game references (cross-surface) | 🟡 | [`docs/plans/2026-06-01-coach-player-games.md`](plans/2026-06-01-coach-player-games.md) | [`scripts/audit-coach-player-games.mjs`](../scripts/audit-coach-player-games.mjs) | `proGameReferences.test.ts`, `playerGames.test.ts`, `lookupPlayerGames.test.ts`, `envelope.test.ts` (toolbelt=24) | 2026-06-01 v1 (pilot: Naroditsky). Gives the coach FULL access to a pro's REAL games for teaching + walkthroughs — the breadth layer alongside hand-narrated model games. Built by `scripts/pro-repertoire/build-game-references.mjs` → `pro-game-references.json` → Dexie (`proGameReferences`, schema v29, prune-on-load). Surfaced via the `playerGames` envelope block + the `lookup_player_games` tool. Audit verifies the Dexie seed + shape on the live bundle; the envelope-event scenario is provider-key-gated. |
| Coach player-game routing (`/coach/teach`) | ✅ | [`docs/plans/2026-06-11-coach-player-game-routing.md`](plans/2026-06-11-coach-player-game-routing.md) | [`scripts/audit-coach-player-game.mjs`](../scripts/audit-coach-player-game.mjs) | `playerGameRequest.test.ts`, `chesscomGamesService.test.ts`, `lookupPlayerGames.magnus-catalan.test.ts` | 2026-06-11. "show me a game that <player> played <opening>" was fuzzy-matched as an OPENING NAME → "did you mean Catalan Opening?" and never reached the lookup. Now a deterministic branch parses the ask + calls `lookup_player_games` as a plain function (no LLM) and WALKS the real game in-page; falls back to a live chess.com archive scan (`/api/chesscom-games`) for games not on disk. 3-instrument prod audit GREEN: S1 magnus-catalan walks a real game (no "did you mean"), S2 off-canonical "how does carlsen play X", S3 plain opening ask does NOT false-positive. |
| Coach grounded self-knowledge Q&A (cross-surface) | ✅ | [`docs/plans/2026-07-04-coach-grounded-verticals.md`](plans/2026-07-04-coach-grounded-verticals.md) | [`scripts/audit-coach-stats-strengths-grounding.mjs`](../scripts/audit-coach-stats-strengths-grounding.mjs), [`scripts/audit-coach-opening-accuracy-grounding.mjs`](../scripts/audit-coach-opening-accuracy-grounding.mjs), [`scripts/audit-coach-opening-traps-grounding.mjs`](../scripts/audit-coach-opening-traps-grounding.mjs), [`scripts/audit-coach-review-due-grounding.mjs`](../scripts/audit-coach-review-due-grounding.mjs) | `questionIntents.test.ts`, `groundedAnswer.test.ts` | 2026-07-04. G0 — "what's my rating/record/win rate", "what am I good at", "how accurate am I in my opening / weakest part to work on", "traps in my strongest opening / what to watch out for / how do you teach these", "what's due for review today" all voiced from COMPUTED data (getOverviewInsights + rating, drillAccuracy/variationAccuracy/weakSpots, trapLines/warningLines, live srsOpeningCards due-counts), never a punt. Detector → pure assembler → `voiceFacts` chokepoint. Paced adversarial probes (off-canonical + collision + break inputs) drive the live prod opening-page chat, classify by reply content: stats/strengths 18/18, opening-accuracy 7/7 grounded. See the plan doc for the pattern + remaining verticals (SRS-due, play-eval threading) + the deferred two-color trap-drill auto-launch. |
| Coach Train (`/coach/train`) | 🟡 | — | [`scripts/audit-untouched-surfaces.mjs`](../scripts/audit-untouched-surfaces.mjs) | `CoachTrainPage.test.tsx` | 2026-05-14. Smoke pass — training heading + one of loading / no-recs / recommendations state. Recommendation click-through pending. |
| Coach Hub (`/coach/home`) | 🟡 | — | [`scripts/audit-untouched-surfaces.mjs`](../scripts/audit-untouched-surfaces.mjs) | `e2e/coach-full-audit.spec.ts` (14 tests) | 2026-05-14. Smoke pass added — 14 tiles render. Per-tile interactive drives still pending. |
| Dashboard + SmartSearchBar (`/`) | ✅ | — | [`scripts/audit-dashboard.mjs`](../scripts/audit-dashboard.mjs) | `DashboardPage.test.tsx`, `DashboardPage.a11y.test.tsx` | 2026-05-14. 17/17 prod audit: root mount, 4 tiles + Import Games, SmartSearchBar typing → dropdown, ask-coach option, all 5 tile nav routes, clear-input. |
| Kid Mode (`/kid`) | 🟡 | — | [`scripts/audit-untouched-surfaces.mjs`](../scripts/audit-untouched-surfaces.mjs) | — | 2026-05-14. Smoke pass — 4 main cards render + Journey card nav works. Fairy-tale / Puzzle Quest / Play Games / Mini-Games sub-flows pending. |
| iOS-specific (AVAudioSession, Bluetooth, mic) | — | — | — | — | Device-only, can't headless audit. |

---

## How to add a new audit

1. **Read the surface end-to-end.** No skimming. Cite line numbers.
2. **Write a UX contract doc** — `docs/<surface>-ux-contract.md`.
   Sections: `SHOULD WORK` (numbered contracts), `AUDIT COVERAGE`
   (what tests verify each contract).
3. **Write a Playwright drive script** —
   `scripts/audit-<surface>.mjs`, mirror
   [`audit-coach-review.mjs`](../scripts/audit-coach-review.mjs) /
   [`audit-coach-chat.mjs`](../scripts/audit-coach-chat.mjs). Single
   Chromium session, SPA nav via clicks, audit-stream POST intercept,
   per-surface `record()` with `expectations` array.
4. **Run against prod.** Identify failed expectations. Fix product
   bugs (don't just patch the test). Add build-time tests for
   data-shape regressions.
5. **Add an E2E spec** (`e2e/<surface>.spec.ts`) when the surface
   warrants Playwright-level CI coverage.
6. **Add `audit-reports/<surface>-*/` to `.gitignore`.**
7. **Add a row to the matrix above.** Status, links, date.
8. **Commit + push.** Vercel auto-deploys; if `git push` fails (PAT
   403), `vercel --prod` is the fallback.

## Audit philosophy (locked in)

- **Surface-mount-only checks are banned.** Every interactive
  affordance must do something visible; verify the visible outcome.
- **Animations: poll-until-stable, not fixed sleep.**
- **For multi-move flows: track WHOSE COLOR moved.**
- **Pull audit-stream events per scenario** to verify expected
  events fired (and unexpected events did not).
- **Sweep, don't spot-fix.** When a bug surfaces, grep the codebase
  for every other instance of the same pattern before declaring done.
- **BEST fix.** Wire dead things, scrap redundancies, don't ship
  cosmetic patches.

- `scripts/audit-samay-deep-prod.mjs` — deep Samay pro-rep audit: every opening × variation tab × WLPP, narration listener + `/api/tts` voice decode, against live prod. Watch + per-tab narration verified in-sandbox; Learn/Practice/Play require a real device (G1.4 ladder-write stall).
scripts/audit-pitfalls-prod.mjs — post-deploy audit for opening pitfalls (common-mistakes). Run: AUDIT_SANDBOX=1 AUDIT_SMOKE_URL=https://chess-academy-pro.vercel.app node scripts/audit-pitfalls-prod.mjs

- `scripts/audit-coaches-library.mjs` — The Coaches Library (`/coach/library`): shelf renders all books, search finds + jumps cross-book, each book opens + reads, live boards render + animate, no page errors.

- `scripts/audit-coach-play-functions.mjs` — Play with Coach (`/coach/play`) FULL function coverage: every control (color/difficulty/time setup, moves, hint, tips+bubble+dismiss, read-position, chat, explore, move-nav, panel resize, takeback, restart, blunder interception + try-best/takeback/continue, resign, skip-to-review). 3-pass contract.

- `scripts/audit-lesson-walkin.mjs` — verifies a curated Watch lesson (`LessonPlayer`) BUILDS its first beat's opening moves one at a time from the start instead of snapping into a deep position (David 2026-06-26 silent walk-in). Opens each opening's Watch, samples "pieces off their home square" fast over ~6s: a SNAP is high+constant from frame 0, a BUILD climbs from 0. `AUDIT_OPENINGS=` overrides the default `sicilian-dragon,caro-kann,ruy-lopez`. Run: `AUDIT_SANDBOX=1 AUDIT_SMOKE_URL=https://chess-academy-pro.vercel.app node scripts/audit-lesson-walkin.mjs` (falls back to localhost when prod Chromium is sandbox-blocked).

- `scripts/audit-coach-move-grounding.mjs` — proves the coach never recommends an illegal move (G0). Drives the live coach (prod, real brain), develops the student's pieces so a redundant developing move is tempting, asks "what should I play here?", and FAILS if any move the coach NAMES (SAN or "[piece] to [square]") is illegal in the position — in particular a move to a square the student's own piece already occupies (the "develop the knight to f3 with a knight on f3" bug). Deterministic chess.js check against the live DOM board. Runner-only (sandbox egress proxy drops browser TLS); dispatch via `post-deploy-audit.yml` scripts input.

- `scripts/audit-coach-play-gaps.mjs` — Play-with-Coach (`/coach/play`) GAP coverage the happy-path audits miss: mid-game control-lock (color/difficulty/time disabled after move 1), phase-narration-fired (audit-stream), play-variation + return-to-game, quiz-wrong-answer resolution, clock-restore-on-resume. Coverage grid + NOT-TESTED reported (quiz/variation depend on an LLM tool call). Runner-only; dispatch via `post-deploy-audit.yml`.
- `scripts/audit-coach-teach-gaps.mjs` — Learn-with-Coach (`/coach/teach`) GAP coverage: inline trap-prompt accept + skip (via the Vienna's punish node), FACE mode (opposite-side counter-line), voice-preference variance (full speaks / silent silenced via narration listener + audit-stream). Coverage grid + NOT-TESTED reported. Runner-only; dispatch via `post-deploy-audit.yml`.
- `scripts/audit-coach-review-gaps.mjs` — Review-with-Coach (`/coach/review/:id`) GAP coverage: ask-panel content + voice-marker strip + board-lock-on-review (no coach-injected move), show-me best-move playout + resume snapback, explore-mode Stockfish reply, corrupt-PGN error UI, deep-link `?move=N` jump + out-of-range clamp, reading-gate (Surface A), and **gap7 grounded citation previews** (Phase 1c — seeds a student-blunder game, asserts `ReviewCitationPreviews` renders cards + the grounded "why better" line + tap-to-jump fires `review-nav`). Replaces the retired interactive blunder-capture prompt assertion with the live auto-mistake-enrollment contract. Coverage grid + NOT-TESTED reported. Runner-only; dispatch via `post-deploy-audit.yml`.
- `scripts/audit-tactics-analysis-practice.mjs` — Analysis Practice (`/tactics/analysis-practice`) end-to-end: empty state (no games), then seed a game → load a position → answer a question → get graded → the computed answer surfaces on a miss → Next advances. Verifies the read-a-position-from-your-games → graded-read wiring. Runner-capable (the grader's LLM + Stockfish answer-key need network) / also runs vs a local dev server; dispatch via `post-deploy-audit.yml`.
- `scripts/audit-reading-upgrades-loop.mjs` — ADVERSARIAL "BREAK IT" LOOP (G7 / 2026-06-12 doctrine) covering EVERY feature shipped in the 2026-06-28 coach/board-reading + all-ages session, in one 3-instrument run (Playwright drives + narration/audit listener sidecar + prod audit-stream liveness): (1) **all-ages** Settings → Coach → Personality has NO flirtatious card + NO profanity/flirt dials (mockery stays); (2) **analysis-practice** two-column rectangle + turn indicator, source toggle, grounded HINT ladder via the Hint button; (3) **setup-trainer** actually STARTS (board + hint button mount after picking a difficulty — the page-help-modal-intercept bug); (4) **tactical-profile** mounts AND scrolls (overflow-y-auto fix); (5) **review citation previews** + grounded "why better" line on a seeded student-blunder game; (6) **tactics-drill** puzzle board mounts. Adversarial: messy AP input (gibberish/emoji/400-char/SQL-ish) + double-submit, cold-nuke IndexedDB on passes ≥2 (first-use), break capture incl React key/update-depth warnings. Per-function coverage grid; 3-consecutive-clean-pass contract. **MET 3/3 on 2026-06-28** (localhost dev = deployed main; prod-in-sandbox can't do app-boot + IndexedDB together — `--disable-web-security` needed for the TLS-MITM proxy denies IDB; voice events need `/api/tts` Vercel fn absent on dev). Run: `AUDIT_SANDBOX=1 AUDIT_SMOKE_URL=http://localhost:5173 node scripts/audit-reading-upgrades-loop.mjs`.
- `scripts/audit-analysis-practice-loop.mjs` — ADVERSARIAL LOOP (G7 doctrine) over BOTH the Analysis Practice drill AND the in-review reading gate: seeds a normal game (any source) + a student-blunder game (mistakes source + the review gate), drives both `analysis-practice-source-{any,mistakes}` toggles, the answer/grade/Next flow, and the review gate (`readingChallengesInReview` on → walk to the mistake → gate before the move → submit/reveal/skip). Throws messy human input (gibberish, emoji, very long, single-char, whitespace, SQL-ish), shuffles + escalates each pass, paces brain-bound submits, captures every break tied to its input (pageerror, app console-error incl React key/update-depth warnings, silent-hang, stuck-input). Per-function coverage grid. 3-consecutive-break-free-pass contract (`NEED_CLEAN=3`, `MAX_PASSES=6`). Runner-only (grader LLM + Stockfish need network); dispatch via `post-deploy-audit.yml` scripts input.
- `scripts/audit-bucket-delivery-loop.mjs` — BUCKET DELIVERY + ORGANIZATION audit (David 2026-07-06 "we need audit tools for bucket delivery and organization"). Runs the invariant engine `src/services/bucketPipelineAudit.ts` against the LIVE app Dexie via `window.__bucketAudit` (installed by `bucketAuditBridge` when `auditStreamUrl` is set). Two controlled scenarios after clearing the weakness stores: (1) CLEAN DELIVERY — a counted slip + its matching mistakePuzzle → `ok=true`, `delivery.withDrill≥1`, `inProfile≥1`, 0 violations; (2) DROP DETECTION — a counted/legal/best-move slip with NO drill → flags `COUNTED_NO_DRILL` (the "picker fired, nothing to drill" class). Backed by the `bucketPipelineAudit.test.ts` unit gate (13 invariants) in ship-check. Run: `AUDIT_SANDBOX=1 AUDIT_SMOKE_URL=https://chess-academy-pro.vercel.app node scripts/audit-bucket-delivery-loop.mjs`.
