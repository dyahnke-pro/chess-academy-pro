build requested 2026-09-02T23:30:52Z — INTERNAL only. Verifies the iOS WASM crash fix on a real device: the analysis worker pool no longer hardcodes stockfish-18-lite-single.js (276 traps in 4 min on /games/import), plus the ios-native runtime-stall demote. Both are proven under mocked iOS conditions only — a real WKWebView has never executed either. Not for Apple until the device confirms.
